node_modules/
.next/
out/
build/
dist/

# Prisma client is generated at build time.
/generated/

# Keep environment variables out of version control
.env
.env.local
.env.*.local

*.log
npm-debug.log*
.DS_Store
.vscode/
.idea/

next-env.d.ts
*.tsbuildinfo

# Runtime data — lives on the host volume, never in git.
data/
uploads/

# Site-specific compose additions (extra_hosts, a different bind address).
docker-compose.override.yml

# The server's address on the VPN. The whole point of the split is that this
# is the one thing not written down in public — including here.
/deploy/split-dns/records.conf

# Generated by `prisma init` — regenerable reference docs for AI assistants.
.agents/
.claude/
.windsurf/
skills-lock.json
